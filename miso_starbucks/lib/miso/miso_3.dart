import 'package:flutter/material.dart';

class Miso3 extends StatelessWidget {
  const Miso3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("miso3"),
    );
  }
}
