import 'package:flutter/material.dart';

class Starbucks3 extends StatelessWidget {
  const Starbucks3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("3"),
      ),
    );
  }
}
